/**
 * Dashboard service boundary.
 *
 * Module 4 intentionally defines no endpoints or mock responses. Module 5 can
 * add dashboard request functions here once the backend contract is approved.
 * Keeping the module export stable prevents UI components from ever importing
 * the request layer directly.
 */
export const dashboardService = Object.freeze({});

export default dashboardService;
